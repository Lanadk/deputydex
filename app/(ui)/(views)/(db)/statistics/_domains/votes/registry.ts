import { StatDefinition } from "@/app/(ui)/(views)/(db)/statistics/_catalog/stat-definition.types";

// Aucun backend "votes" n'existe encore (ni repository, ni use-case) —
// slot présent dans le pattern, catalogue vide jusqu'à son implémentation.
export const VOTES_STATS: StatDefinition[] = [];
